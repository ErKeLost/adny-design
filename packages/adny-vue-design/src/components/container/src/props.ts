const asideProps = {
  width: {
    type: String,
    default: null
  }
}
const footerProps = {
  height: { type: String, default: null }
}

const mainProps = {}

const headerProps = {
  height: { type: String, default: null }
}

const containerProps = {
  direction: { type: String, default: '' }
}

export { asideProps, mainProps, headerProps, footerProps, containerProps }
